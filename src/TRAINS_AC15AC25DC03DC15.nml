 if (railtype_available("SAAa") || railtype_available("SAAA") || railtype_available("SAAd") || railtype_available("SAAD"))
 {
 	item (FEAT_TRAINS, item_CC40100)
    {
 		property
        {
 			track_type: [AC15, AC25, DC03, DC15];
  		}
    }
 }
 else
 {
 	item (FEAT_TRAINS, item_CC40100)
    {
 		property
        {
 			track_type: ACDC;
 		}
 	}
 }